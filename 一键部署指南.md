# 🚀 充易达系统v2.0 - 真正的一键部署

## 方法：上传部署脚本并执行

您只需要做一件事：**在服务器上运行一个脚本**

---

## 📋 操作步骤（仅需3步）

### 步骤1：连接服务器

打开Git Bash或PowerShell，执行：

```bash
ssh ubuntu@81.70.208.147
```

输入密码：`Aa462300`

### 步骤2：下载并执行部署脚本

连接成功后，执行以下命令（复制粘贴即可）：

```bash
cd /tmp && curl -O https://raw.githubusercontent.com/pengx2301-art/cydzhanlue/main/deploy-remote.sh && chmod +x deploy-remote.sh && ./deploy-remote.sh
```

### 步骤3：等待部署完成

脚本会自动：
- ✅ 备份旧文件
- ✅ 安装系统依赖（Node.js、npm、pm2）
- ✅ 从GitHub克隆最新代码
- ✅ 安装项目依赖
- ✅ 启动服务
- ✅ 配置防火墙

大约需要 3-5 分钟完成。

---

## 🎯 部署完成后

### 访问地址

打开浏览器访问：
- 商品管理：http://81.70.208.147:8899/product-connection.html
- 快速下单：http://81.70.208.147:8899/order-page.html

### 查看服务状态

```bash
pm2 status
pm2 logs cyd-admin
```

### 常用命令

```bash
# 重启服务
pm2 restart cyd-admin

# 停止服务
pm2 stop cyd-admin

# 查看日志
pm2 logs cyd-admin

# 查看详细信息
pm2 describe cyd-admin
```

---

## 🔄 后续更新

以后有新功能更新，只需要：

```bash
cd /app
git clone git@github.com:pengx2301-art/cydzhanlue.git temp
cp temp/*.js temp/*.json temp/*.html .
rm -rf temp
pm2 restart cyd-admin
```

就这么简单！

---

## ⚠️ 如果方法1失败

如果curl下载失败，可以用这个方法：

```bash
cd /tmp
nano deploy-remote.sh
```

然后从本地文件 `c:/Users/60496/WorkBuddy/Claw/deploy-remote.sh` 复制内容，保存后执行：

```bash
chmod +x deploy-remote.sh
./deploy-remote.sh
```

---

## 📊 完整的自动化部署流程

```
1. 连接服务器 (SSH)
   ↓
2. 下载部署脚本 (curl)
   ↓
3. 执行部署脚本
   ├─ 备份旧文件
   ├─ 安装依赖
   ├─ 拉取代码
   ├─ 安装npm包
   └─ 启动服务
   ↓
4. 访问系统
```

---

## ✅ 验证部署

```bash
# 检查服务是否运行
pm2 status

# 应该看到 cyd-admin 状态为 online

# 测试API
curl http://localhost:8899/api/health

# 应该返回：{"status":"healthy","version":"2.0.0"}
```

---

## 🎉 总结

**真正的自动化部署**：
- ✅ 不需要手动上传文件
- ✅ 不需要手动安装依赖
- ✅ 不需要手动配置服务
- ✅ 完全自动化，只需执行一条命令

**以后更新**：
- ✅ 只需要3条命令
- ✅ 代码已推送到GitHub
- ✅ 直接从GitHub拉取

**现在就开始吧！** 🚀

---

**有问题？**
查看日志：`pm2 logs cyd-admin`
检查配置：`cat /app/product-connection.js`
