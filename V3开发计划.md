# 充易达系统 v3.0.0 开发计划

## 🎯 版本目标

开发用户和代理专属后台系统，新增自来单功能，优化供货商管理。

---

## 📋 已完成的后端功能

### 1. 用户后台模块 ✅
**文件**: `user-backend.js`

**功能**:
- ✅ 用户登录认证
- ✅ 用户仪表盘数据（总消费、余额、订单数、今日订单）
- ✅ 获取可用商品列表
- ✅ 单个商品下单
- ✅ 批量下单
- ✅ 申请加款
- ✅ 查看加款申请记录
- ✅ 查看用户订单列表
- ✅ 获取订单详情

### 2. 自来单管理模块 ✅
**文件**: `zilaidan-manager.js`

**功能**:
- ✅ 配置对接平台（蜜蜂汇云、客客帮）
- ✅ 获取蜜蜂汇云订单（作为卖家）
- ✅ 获取客客帮订单（作为供货商）
- ✅ 处理订单充值
- ✅ 批量处理订单
- ✅ 获取所有自来单
- ✅ 蜜蜂汇云API对接（框架，待完善）
- ✅ 客客帮API对接（框架，待完善）

### 3. 供货商管理模块 ✅
**文件**: `supplier-manager.js`

**功能**:
- ✅ 获取所有供货商
- ✅ 添加供货商（包含接口配置）
- ✅ 更新供货商
- ✅ 删除供货商
- ✅ 启用/禁用供货商
- ✅ 测试接口连接
- ✅ 刷新供货商状态
- ✅ 批量刷新所有供货商状态
- ✅ 获取供货商统计信息
- ✅ 搜索供货商
- ✅ 获取可用供货商

### 4. API服务器 v3.0.0 ✅
**文件**: `api-server-v3.js`

**路由**:
- ✅ `/api/user/*` - 用户后台API
- ✅ `/api/admin/*` - 总后台API
- ✅ `/api/zilaidan/*` - 自来单API
- ✅ `/api/supplier/*` - 供货商管理API

---

## 🔧 待完善的功能

### 1. 蜜蜂汇云API对接
**状态**: 框架已创建，需要实际API文档

**需要**:
- 蜜蜂汇云开发文档
- API端点地址
- 认证方式
- 订单数据格式

### 2. 客客帮API对接
**状态**: 框架已创建，需要实际API文档

**需要**:
- 客客帮开发文档
- API端点地址
- 认证方式
- 订单数据格式

---

## 📝 前端开发计划

### 1. 用户后台页面
**路径**: `/user/*`

**页面**:
- [ ] 用户登录页面 (`/user/login.html`)
- [ ] 用户仪表盘 (`/user/dashboard.html`)
- [ ] 商品列表页面 (`/user/products.html`)
- [ ] 单个下单页面 (`/user/order-single.html`)
- [ ] 批量下单页面 (`/user/order-bulk.html`)
- [ ] 我的订单页面 (`/user/orders.html`)
- [ ] 加款申请页面 (`/user/topup.html`)
- [ ] 加款记录页面 (`/user/topup-history.html`)

### 2. 总后台页面
**路径**: `/admin/*`

**页面**:
- [ ] 自来单管理 (`/admin/zilaidan.html`)
- [ ] 加款审核 (`/admin/topup-review.html`)
- [ ] 供货商管理 (`/admin/suppliers.html`)

---

## 🔄 待完成的前端页面

由于响应长度限制，前端页面将在下一个会话继续开发。

---

## 📦 数据库设计

### 新增表

#### 用户表 (users)
```sql
CREATE TABLE users (
    id INT PRIMARY KEY AUTO_INCREMENT,
    username VARCHAR(50) UNIQUE NOT NULL,
    password VARCHAR(255) NOT NULL,
    balance DECIMAL(10,2) DEFAULT 0,
    role ENUM('user', 'agent') DEFAULT 'user',
    status ENUM('active', 'disabled') DEFAULT 'active',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

#### 加款申请表 (topup_applications)
```sql
CREATE TABLE topup_applications (
    id VARCHAR(50) PRIMARY KEY,
    user_id INT NOT NULL,
    amount DECIMAL(10,2) NOT NULL,
    proof TEXT,
    remark TEXT,
    status ENUM('pending', 'approved', 'rejected') DEFAULT 'pending',
    admin_remark TEXT,
    processed_by INT,
    processed_at TIMESTAMP NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id)
);
```

#### 供货商表 (suppliers)
```sql
CREATE TABLE suppliers (
    id VARCHAR(50) PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    type ENUM('product', 'service', 'both') NOT NULL,
    api_config JSON NOT NULL,
    enabled BOOLEAN DEFAULT TRUE,
    available BOOLEAN DEFAULT FALSE,
    last_check_time TIMESTAMP NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

#### 自来单表 (zilaidan_orders)
```sql
CREATE TABLE zilaidan_orders (
    id VARCHAR(50) PRIMARY KEY,
    platform VARCHAR(50) NOT NULL,
    platform_role ENUM('seller', 'supplier') NOT NULL,
    product_name VARCHAR(255) NOT NULL,
    amount DECIMAL(10,2) NOT NULL,
    status ENUM('pending', 'completed', 'failed') DEFAULT 'pending',
    processed_at TIMESTAMP NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

---

## 🚀 部署说明

### 1. 上传新文件到服务器
```bash
scp -i C:/Users/60496/Desktop/chongyida.pem \
    c:/Users/60496/WorkBuddy/Claw/user-backend.js \
    c:/Users/60496/WorkBuddy/Claw/zilaidan-manager.js \
    c:/Users/60496/WorkBuddy/Claw/supplier-manager.js \
    c:/Users/60496/WorkBuddy/Claw/api-server-v3.js \
    ubuntu@81.70.208.147:/app/
```

### 2. 在服务器上执行
```bash
ssh -i C:/Users/60496/Desktop/chongyida.pem ubuntu@81.70.208.147

cd /app

# 安装新增依赖
npm install

# 备份旧版本
cp api-server.js api-server.js.backup

# 启用新版本
mv api-server-v3.js api-server.js

# 重启服务
pm2 restart api-server
```

---

## 📊 功能对比

| 功能 | v2.0.0 | v3.0.0 |
|------|--------|--------|
| 用户后台 | ❌ | ✅ |
| 用户仪表盘 | ❌ | ✅ |
| 单个下单 | ✅ | ✅ |
| 批量下单 | ✅ | ✅ |
| 加款申请 | ❌ | ✅ |
| 加款审核 | ❌ | ✅ |
| 自来单管理 | ❌ | ✅ |
| 蜜蜂汇云对接 | ❌ | 🔄 |
| 客客帮对接 | ❌ | 🔄 |
| 供货商接口配置 | ❌ | ✅ |
| 供货商接口测试 | ❌ | ✅ |

---

## ⏳ 下一步

1. **提供API文档**
   - 蜜蜂汇云开发文档
   - 客客帮开发文档

2. **开发前端页面**
   - 用户后台页面
   - 总后台页面

3. **完善API对接**
   - 根据实际文档完善蜜蜂汇云API
   - 根据实际文档完善客客帮API

4. **测试功能**
   - 用户后台测试
   - 自来单功能测试
   - 供货商管理测试

---

**创建时间**: 2026年3月19日
**版本**: v3.0.0
**状态**: 后端完成，待完善API对接和前端开发
