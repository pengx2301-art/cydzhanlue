# v3.3.0 云服务器部署文档

## 📋 部署信息

- **版本**: v3.3.0
- **服务器**: 81.70.208.147
- **端口**: 8899
- **部署日期**: 2026-03-19

---

## 🚀 快速部署

### 方法1: 使用部署脚本（推荐）

```bash
# 在Windows上运行部署脚本
部署云服务器.bat
```

脚本会自动完成以下操作：
1. ✅ 推送代码到GitHub
2. ✅ 检查服务器连接
3. ✅ 备份服务器文件
4. ✅ 上传新文件到服务器
5. ✅ 重启API服务器

---

### 方法2: 手动部署

#### 步骤1: 推送代码到GitHub
```bash
cd c:/Users/60496/WorkBuddy/Claw
git push
```

#### 步骤2: 上传文件到服务器
```bash
# 上传API服务器
scp api-server-v3.2.js root@81.70.208.147:/root/Claw/api-server.js

# 上传管理后台
scp admin-v3.2.js root@81.70.208.147:/root/Claw/admin.js

# 上传登录页面
scp user/login.html root@81.70.208.147:/root/Claw/user/login.html
scp admin/login.html root@81.70.208.147:/root/Claw/admin/login.html

# 上传供货商模块
scp kasushou-supplier.js root@81.70.208.147:/root/Claw/
scp kayixin-supplier.js root@81.70.208.147:/root/Claw/
scp kekebang-supplier.js root@81.70.208.147:/root/Claw/
scp supplier-manager-v3.js root@81.70.208.147:/root/Claw/supplier-manager.js
```

#### 步骤3: 重启API服务器
```bash
ssh root@81.70.208.147 "cd /root/Claw && pm2 restart api-server"
```

或使用PM2启动：
```bash
ssh root@81.70.208.147 "cd /root/Claw && pm2 start api-server.js --name api-server --watch"
```

---

## 📦 部署文件清单

### 核心文件
| 文件 | 用途 | 目标路径 |
|------|------|----------|
| api-server-v3.2.js | API服务器 | /root/Claw/api-server.js |
| admin-v3.2.js | 管理后台JS | /root/Claw/admin.js |
| supplier-manager-v3.js | 供货商管理器 | /root/Claw/supplier-manager.js |

### 登录页面
| 文件 | 用途 | 目标路径 |
|------|------|----------|
| user/login.html | 用户登录页面 | /root/Claw/user/login.html |
| admin/login.html | 管理员登录页面 | /root/Claw/admin/login.html |

### 供货商模块
| 文件 | 用途 | 目标路径 |
|------|------|----------|
| kasushou-supplier.js | 卡速售供货商 | /root/Claw/kasushou-supplier.js |
| kayixin-supplier.js | 卡易信供货商 | /root/Claw/kayixin-supplier.js |
| kekebang-supplier.js | 客客帮供货商 | /root/Claw/kekebang-supplier.js |

---

## 🔧 服务器配置

### PM2配置
```bash
# 查看API服务器状态
ssh root@81.70.208.147 "pm2 status"

# 查看日志
ssh root@81.70.208.147 "pm2 logs api-server"

# 重启服务器
ssh root@81.70.208.147 "pm2 restart api-server"

# 停止服务器
ssh root@81.70.208.147 "pm2 stop api-server"
```

### 防火墙配置
确保以下端口已开放：
- **8899**: HTTP服务端口
- **22**: SSH端口

---

## 🌐 访问地址

### 用户后台
- **登录页面**: http://81.70.208.147:8899/user/login.html
- **用户后台**: http://81.70.208.147:8899/user-backend.html

### 管理员后台
- **登录页面**: http://81.70.208.147:8899/admin/login.html
- **管理后台**: http://81.70.208.147:8899/admin.html

### API接口
- **健康检查**: http://81.70.208.147:8899/api/health
- **版本信息**: http://81.70.208.147:8899/api/version
- **用户登录**: http://81.70.208.147:8899/api/user/login
- **管理员登录**: http://81.70.208.147:8899/api/admin/login

---

## ✅ 部署验证

### 1. 检查API服务器状态
```bash
ssh root@81.70.208.147 "pm2 status"
```

预期输出：
```
┌────┬─────────────────┬──────────────┬─────────────┐
│ id │ name            │ version      │ mode        │
├────┼─────────────────┼──────────────┼─────────────┤
│ 0  │ api-server      │ 3.3.0        │ fork        │
└────┴─────────────────┴──────────────┴─────────────┘
```

### 2. 测试健康检查
```bash
curl http://81.70.208.147:8899/api/health
```

预期输出：
```json
{
  "status": "ok",
  "version": "v3.2.0",
  "timestamp": "2026-03-19T12:00:00.000Z"
}
```

### 3. 访问登录页面
- 打开浏览器访问 http://81.70.208.147:8899/user/login.html
- 打开浏览器访问 http://81.70.208.147:8899/admin/login.html

---

## 📊 供货商配置

### 数据库配置
需要在服务器数据库中配置供货商信息：

#### 卡速售
```sql
INSERT INTO suppliers (name, type, api_url, api_key, api_secret, status, created_at)
VALUES ('卡速售', 'kasushou', 'https://api.kasushou.com/api/v2', 'your_appkey', 'your_appsecret', 1, NOW());
```

#### 卡易信
```sql
INSERT INTO suppliers (name, type, api_url, api_key, api_secret, status, created_at)
VALUES ('卡易信', 'kayixin', 'http://api.kayixin.com', 'your_username', 'your_password', 1, NOW());
```

#### 客客帮
```sql
INSERT INTO suppliers (name, type, api_url, api_key, api_secret, status, created_at)
VALUES ('客客帮', 'kekebang', 'https://purchase.kekebang.com.cn', 'your_appkey', 'your_appsecret', 1, NOW());
```

---

## 🔍 故障排查

### 问题1: API服务器无法启动
```bash
# 查看错误日志
ssh root@81.70.208.147 "pm2 logs api-server --lines 50"

# 检查端口占用
ssh root@81.70.208.147 "netstat -tlnp | grep 8899"

# 检查Node.js版本
ssh root@81.70.208.147 "node --version"
```

### 问题2: 文件上传失败
```bash
# 检查SSH连接
ssh root@81.70.208.147 "echo 'Connection OK'"

# 检查文件权限
ssh root@81.70.208.147 "ls -la /root/Claw"

# 修复权限
ssh root@81.70.208.147 "chmod +x /root/Claw/*.js"
```

### 问题3: 供货商API调用失败
```bash
# 检查网络连接
ssh root@81.70.208.147 "curl -I https://api.kasushou.com"

# 查看API日志
ssh root@81.70.208.147 "pm2 logs api-server | grep 'supplier'"

# 检查供货商配置
ssh root@81.70.208.147 "mysql -u root -p'your_password' claw -e 'SELECT * FROM suppliers'"
```

---

## 📈 性能监控

### PM2监控
```bash
# 实时监控
ssh root@81.70.208.147 "pm2 monit"

# 查看资源使用
ssh root@81.70.208.147 "pm2 show api-server"
```

### 日志查看
```bash
# 查看最近100行日志
ssh root@81.70.208.147 "pm2 logs api-server --lines 100"

# 查看错误日志
ssh root@81.70.208.147 "pm2 logs api-server --err"

# 清空日志
ssh root@81.70.208.147 "pm2 flush"
```

---

## 🔄 回滚方案

如果部署出现问题，可以快速回滚：

```bash
# 1. 恢复备份文件
ssh root@81.70.208.147 "cd /root/Claw && cp -r backup/* ."

# 2. 重启API服务器
ssh root@81.70.208.147 "pm2 restart api-server"

# 3. 验证服务状态
curl http://81.70.208.147:8899/api/health
```

---

## 📝 部署检查清单

部署前检查：
- [ ] 代码已提交到Git
- [ ] Git推送到GitHub成功
- [ ] 服务器SSH连接正常
- [ ] 服务器备份已创建
- [ ] 供货商API密钥已配置

部署后验证：
- [ ] API服务器启动成功
- [ ] 健康检查通过
- [ ] 用户登录页面可访问
- [ ] 管理员登录页面可访问
- [ ] 供货商API可正常调用
- [ ] 日志无错误信息

---

## 📞 技术支持

如遇到问题，请检查：
1. PM2日志：`pm2 logs api-server`
2. 系统日志：`/var/log/syslog`
3. Nginx日志（如使用）：`/var/log/nginx/`

---

**v3.3.0 部署完成！** 🎉

**所有功能已上线！**
